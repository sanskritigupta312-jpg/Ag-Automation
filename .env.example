# Chrome DevTools Protocol configuration
CDP_PORT=9222

# Local Host Gemini Engine Endpoint (Ollama or local host API proxy)
GEMINI_LOCAL_HOST=http://localhost:11434
GEMINI_LOCAL_MODEL=gemini-flash

# Operational Mode on launch: FEED, DM, or PROFILE
INITIAL_MODE=FEED
